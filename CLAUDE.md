# 작업 규칙

## 사용자에게 무언가를 요청할 때

사람 손이 필요한 일(가입, 인증, 키 발급, 설치, 승인)이 생기면 **말로만 설명하지 않는다.**
매번 다음 세 가지를 갖춰서 넘긴다.

1. **단계별 순서** — 번호를 매기고, 각 단계에 직접 링크와 입력할 내용을 적는다.
   "가입하세요" 가 아니라 "이 주소에서 이메일로 가입 → 닉네임은 나중에 바꿀 수 있으니
   아무거나 → 다음" 수준까지 쪼갠다.
2. **막히는 지점** — 그 단계에서 흔히 걸리는 것과 걸렸을 때의 해법을 미리 적는다.
3. **Claude in Chrome 용 프롬프트** — 사용자의 PC Claude Code 세션에 그대로 붙여넣으면
   브라우저를 대신 몰아줄 문장. 그 세션은 이 대화를 모르므로 혼자서도 말이 되게 쓴다.

### 왜 이렇게 하는가

이 저장소의 작업은 원격 컨테이너에서 돈다. 사용자의 브라우저에 닿을 수 없고,
외부 사이트 대부분이 egress 정책에 막혀 있다. 그래서 "대신 해드릴게요" 가 성립하지 않는다.

대신 할 수 있는 건 **사용자가 눌러야 할 횟수를 줄이는 것**이다. 정확한 링크, 정확한 순서,
그리고 로컬 Claude Code + Chrome 확장이 실제로 클릭할 수 있는 프롬프트까지 준비해두면
사용자가 하는 일은 본인 확인이 필요한 입력뿐이다.

### 대신할 수 없는 것은 분명히 말한다

휴대폰 SMS 인증, 신분증 촬영, 사업자등록증 업로드, 정산 계좌 입력은 명의가 걸린 일이라
누구도 대신하지 않는다. Chrome 확장을 붙여도 마찬가지다. 줄어드는 건 폼을 찾아다니는
시간이지 입력 자체가 아니다 — 이걸 흐리게 말하지 않는다.

### 요청은 한 장으로 모은다

여러 건을 요청할 때는 채팅에 늘어놓지 말고 Artifact 한 장에 모은다.
리드타임이 긴 것부터 번호를 매기고, 체크 상태를 기억하게 하고, 남은 대기 시간을
계산해 보여준다. 사용자가 "지금 뭘 해야 하지" 를 다시 묻지 않아도 되게 한다.

## 비밀값

토큰·API 키·세션 쿠키를 채팅에 붙여넣게 하지 않는다. 받을 일이 생기면
GitHub Secrets 에 넣을 이름만 알려주고, 값은 보지 않는다.

디버깅에 응답 형식이 필요하면 **값을 감추고 구조만 출력하는 도구**를 만들어 준다
(`src/probe-typecast.ts` 참고). 그 출력은 그대로 공유해도 안전해야 한다.

## 실패를 삼키지 않는다

스크래퍼가 깨지거나 링크 부착이 실패하면 빈 값으로 넘어가지 않는다.
무엇을 하다 실패했는지 말하고 던진다. 조용한 실패는 "영상은 나갔는데 수익이 0" 으로
며칠 뒤에 돌아온다.

## 설정이 거짓말하지 않게 한다

주석이 약속한 동작이 구현돼 있지 않으면 주석을 고치는 게 아니라 그 설정을 없앤다.
설정을 믿고 내린 판단이 전부 틀어지기 때문이다.
